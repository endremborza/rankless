# Polyglot repo: ignore everything by default, then re-include the
# hand-written SvelteKit frontend + project docs/config.
/*

!/src
!/tests
!/docs
!/*.md
!/*.json
!/*.ts
!/*.js
!/*.cjs

# Carve-outs inside the re-included trees: generated data, reports, lockfiles
/src/lib/assets/data
/docs/*.html
/docs/benchmarking
package-lock.json
bun.lock

# Markdown: prettier column-pads tables, which rewrites every row on a one-cell edit
*.md
